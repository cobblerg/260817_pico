# 피코 WH LED AI 시뮬레이터 PRD

## 1\. 문제(병목)

중학생이 라즈베리파이 피코 WH의 기본적인 하드웨어 연결과 Python 제어 원리를 실제 장비 없이 웹에서 경험할 수 있는 간단한 가상 시뮬레이터가 필요하다.

수업 시간에 복잡한 회로 구성이나 Python 전체 문법을 학습하기보다 **LED 연결 → 코드 수정 → 실행 → 결과 확인 → 오류 수정**의 핵심 학습 흐름을 빠르게 경험하는 것을 목표로 한다.

\---

## 2\. 대상 사용자

* 중학생
* 라즈베리파이 피코 WH 입문자
* Python의 `Pin`, `toggle()`, `sleep()` 개념을 처음 학습하는 학생

별도의 회원가입이나 로그인 없이 링크를 통해 바로 사용할 수 있도록 한다.

\---

## 3\. MVP — 3시간 내 반드시 동작할 한 가지

### 핵심 목표

**LED 1개를 Pico WH에 연결하고 제한된 Python 명령으로 LED를 점멸시키는 웹 시뮬레이터**

MVP에서 지원하는 부품은 **LED 1개**로 제한한다.

학생은 다음 과정을 수행할 수 있어야 한다.

1. 부품 팔레트의 LED를 보드 캔버스로 가져온다.
2. LED의 연결점에서 **선을 드래그하여 Pico WH의 GP15에 연결**한다.
3. GND 연결선도 드래그하여 연결한다.
4. 코드 에디터에서 제공된 기본 코드를 수정한다.
5. `▶ 실행`을 누른다.
6. 코드에 따라 LED가 켜지고 꺼지는 것을 확인한다.
7. 오류가 있으면 콘솔에서 오류 내용을 확인한다.
8. 회로와 코드를 저장하고 공유 링크를 생성한다.

\---

## 4\. MVP에서 지원하는 Python 명령

Python 전체를 실행하지 않고 교육에 필요한 제한된 명령만 해석한다.

### 지원 범위

```python
from machine import Pin
from time import sleep

led = Pin(15, Pin.OUT)

while True:
    led.toggle()
    sleep(1)
```

해석 대상은 다음으로 제한한다.

* `Pin(15, Pin.OUT)`
* `led.toggle()`
* `sleep(n)`
* `while True`

`sleep()`의 숫자를 변경하면 LED 점멸 간격이 변경된다.

Python 전체 인터프리터 구현은 MVP 범위에서 제외한다.

\---

## 5\. CONTEXT — AI에게 줄 맥락

중학생 대상의 라즈베리파이 피코 WH 입문용 웹 시뮬레이터다.

실제 전자회로를 정밀하게 재현하는 것이 아니라 다음 개념을 이해시키는 것이 목적이다.

**부품 연결 → 핀 번호 → Python 코드 → LED 동작**

학생이 복잡한 설정 없이 바로 실습할 수 있도록 UI와 기능을 최소화한다.

\---

## 6\. 사용자 시나리오

학생이 수업 링크로 접속하면 로그인 없이 바로 작업 화면이 열린다.

왼쪽 부품 팔레트의 LED를 가운데 보드 캔버스로 드래그한다. LED의 신호 연결점에서 선을 끌어 Pico WH의 **GP15**에 연결하고, GND 연결점도 같은 방식으로 GND 핀에 연결한다.

오른쪽 코드 에디터에는 다음 기본 코드가 미리 입력되어 있다.

```python
from machine import Pin
from time import sleep

led = Pin(15, Pin.OUT)

while True:
    led.toggle()
    sleep(1)
```

학생은 주로 `sleep()` 값을 변경하여 LED 점멸 패턴을 바꾼다.

`▶ 실행`을 누르면 시뮬레이터가 지원 명령을 해석하여 캔버스의 LED를 점멸시킨다.

코드에 문제가 있으면 실행을 중단하고 콘솔에 오류 위치와 원인을 학생이 이해할 수 있는 표현으로 표시한다.

작업이 완료되면 `저장`을 눌러 회로 상태와 코드를 Firebase에 저장하고 공유 링크를 생성한다.

\---

## 7\. 화면 구성

### 작업 화면

```text
┌──────────────────────────────────────────────────────┐
│               🫐 피코 LED 시뮬레이터                 │
│          선을 연결하고 Python으로 제어하기            │
├──────────┬──────────────────────┬────────────────────┤
│   부품   │      보드 캔버스      │    코드 에디터     │
│          │                      │                    │
│  ○ LED   │  LED ○──────○ GP15  │ from machine ...  │
│          │      │               │ from time ...     │
│          │      └──────○ GND    │                    │
│          │                      │ led=Pin(15,...)    │
│          │    ┌───────────┐     │ while True:        │
│          │    │  Pico WH  │     │   led.toggle()     │
│          │    └───────────┘     │   sleep(1)         │
├──────────┴──────────────────────┴────────────────────┤
│ \[▶ 실행] \[■ 정지] \[↺ 초기화] \[💾 저장]  상태: 대기중 │
├──────────────────────────────────────────────────────┤
│ 콘솔                                                 │
│ > 실행 준비 완료                                     │
└──────────────────────────────────────────────────────┘
```

\---

## 8\. 연결 방식

기존의 핀 선택 팝업 방식은 사용하지 않는다.

학생이 **연결선을 직접 드래그하여 핀과 연결하는 방식**으로 통일한다.

### 연결 순서

```text
LED 신호점 ○ ─────드래그────→ ○ GP15

LED GND점  ○ ─────드래그────→ ○ GND
```

연결에 성공하면 선을 화면에 유지하고 연결된 핀을 시각적으로 표시한다.

잘못된 핀에 연결하면 해당 선을 거부하거나 오류 상태로 표시한다.

\---

## 9\. 저장 및 공유

저장 대상은 다음 두 가지로 제한한다.

* LED 연결 상태
* 작성한 코드

Firebase에 데이터를 저장한 뒤 랜덤 ID 기반 공유 링크를 생성한다.

예:

```text
https://example.netlify.app/p/a7f2x9
```

### 개인정보 보호

학생의 다음 정보는 요구하지 않는다.

* 이름
* 학급
* 학번
* 이메일

저장 시 기본 이름은 자동 생성한다.

예:

```text
작품 이름: LED-A7F2
```

학생이 별칭을 입력할 수는 있지만 실명이나 학급 입력을 유도하지 않는다.

화면에도 다음 안내를 표시한다.

> 개인정보 보호를 위해 이름·학급·학번을 입력하지 마세요.

\---

## 10\. 테스트 케이스

### 정상 시나리오

**TC-01 LED 연결**

LED 신호선을 GP15에 연결하고 GND선을 GND에 연결하면 연결선이 유지되고 LED가 연결 상태로 표시되어야 한다.

**TC-02 LED 점멸**

`Pin(15)`와 `sleep(1)`을 사용하는 코드를 실행하면 LED가 1초 간격으로 점멸하고 상태가 `실행 중`으로 변경되어야 한다.

**TC-03 저장 및 공유**

저장 버튼을 누르면 Firebase에 회로와 코드가 저장되고 랜덤 ID 기반 공유 링크가 생성되어야 한다.

\---

### 실패 시나리오

**TC-04 빈 코드 실행**

코드가 비어 있을 때 실행하면 실행하지 않고 다음 메시지를 표시한다.

```text
먼저 코드를 작성해 주세요.
```

**TC-05 코드 문법 오류**

괄호 등이 잘못된 코드를 실행해도 브라우저가 멈추거나 흰 화면이 되지 않아야 한다.

콘솔에 오류가 발생한 줄과 원인을 표시한다.

**TC-06 sleep 없는 무한 반복**

`while True` 내부에 `sleep()`이 없으면 브라우저가 멈추지 않도록 실행을 차단하거나 자동 중단한다.

```text
sleep이 없어 실행을 중단했어요.
```

**TC-07 연결되지 않은 핀 제어**

LED가 GP15에 연결되어 있지 않은 상태에서 `Pin(15)`를 실행하면 다음과 같이 안내한다.

```text
GP15에 연결된 LED가 없어요.
```

**TC-08 Firebase/network 저장 실패**

저장 중 Firebase 오류 또는 네트워크 단절이 발생하면 현재 캔버스와 코드 내용을 삭제하거나 초기화하지 않는다.

```text
저장하지 못했습니다.
현재 작업은 그대로 유지됩니다.
네트워크 연결을 확인한 뒤 다시 시도하세요.
```

학생이 다시 `저장`을 눌러 재시도할 수 있어야 한다.

**TC-09 잘못된 공유 링크**

존재하지 않거나 삭제·손상된 공유 ID로 접근하더라도 흰 화면이나 무한 로딩이 발생해서는 안 된다.

```text
저장된 작품을 찾을 수 없습니다.

\[새로 시작]
```

`새로 시작`을 선택하면 정상적인 초기 작업 화면으로 복구되어야 한다.

\---

## 11\. P1 — 오늘은 만들지 않을 것

다음 기능은 3시간 MVP에서 제외한다.

* 버튼
* 부저
* 각종 센서
* 여러 LED 동시 연결
* 자유로운 모든 GPIO 핀 지원
* 실제 MicroPython 전체 실행
* Python 전체 문법 검사
* 복잡한 전자회로 시뮬레이션
* 저항값 계산
* 학생 회원가입 및 로그인
* 학생별 학습 이력
* 학생 개인별 누적 관리
* 교사용 대시보드
* 과제 제출 관리

\---

## 12\. 기술 스택

* **Frontend:** 웹앱
* **Hosting:** Netlify
* **Database:** Firebase
* **로그인:** 없음
* **저장:** 랜덤 ID 기반 작품 저장
* **시뮬레이션:** 제한된 `Pin / toggle / sleep` 명령 파싱 및 실행

\---

## 13\. MVP 완료 기준

12시까지 다음 흐름이 처음부터 끝까지 한 번 정상적으로 동작하면 MVP를 완료한 것으로 판단한다.

**접속 → LED 배치 → 선을 GP15/GND에 연결 → 코드 수정 → 실행 → LED 점멸 → 오류 표시 → 저장 → 공유 링크 생성 → 공유 링크로 작품 다시 열기**

핵심 판단 기준은 **실제 Pico WH를 정밀하게 재현했는가가 아니라, 학생이 연결과 코드의 관계를 직접 확인할 수 있는가​**이다.

